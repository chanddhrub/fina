# fina
Transaction Analytics app for everyone

## Expo Go testing

Run the app in Expo Go with:

```bash
npm run expo-go
```

If Metro cache gets stale:

```bash
npm run expo-go:clear
```

For a fast validation step before opening Expo Go:

```bash
npm run typecheck
```
